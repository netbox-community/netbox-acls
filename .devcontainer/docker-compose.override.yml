---
version: '3.4'
services:
  netbox:
    image: netbox-plugin_dev
    build:
      dockerfile: Dockerfile-plugin_dev
    ports:
      - 8000:8080
    command: ["/bin/sh", "-c", "while true; do sleep 30; done;"]
    volumes:
      - ../:/opt/netbox/netbox/netbox-access-lists
      - ../plugins.py:/etc/netbox/plugins.py:z,ro
      - ~/.gitconfig:/root/.gitconfig:z,ro
      - ~/.ssh:/root/.ssh
    # - /var/run/docker.sock:/var/run/docker.sock
    #  - ../:/opt/netbox/sample_plugin
    #  - ./.bashrc:/opt/netbox/sample_plugin/.bashrc
    #  - ./.bash_profile:/opt/netbox/sample_plugin/.bash_profile
